@model IEnumerable<DBapp.Models.attendance>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@using (Html.BeginForm("Index", "attendances", FormMethod.Post))
{
    <div>
        Search @Html.TextBox("searchString")
        <input id="Submit1" type="submit" value="Filter" />

    </div>
}
<table class="table">
    <tr>
        <th>
            Attended
        </th>
        <th>
            Date
        </th>
        <th>
            Class Level
        </th>
        <th>
            Student Name
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.attendance_attended)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.attendance_date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.classs.class_level)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.student.student_name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.attendance_id }) |
                @Html.ActionLink("Details", "Details", new { id = item.attendance_id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.attendance_id })
            </td>
        </tr>
    }

</table>
